[pytest]
addopts = -q --strict-markers --tb=short
testpaths = tests
python_files = test_*.py
pythonpath =
    src
    .
markers =
    postgres: requires the PostgreSQL integration service
    playwright_mcp_live: requires the official Playwright MCP process and a browser
