schema_version: agentic-qa.harness.tool-manifest.v2
name: evidence.read
provider: builtin
description: 读取并校验结构化执行证据
input_schema:
  type: object
  required: [path]
  properties:
    path: {type: string, minLength: 1}
  additionalProperties: false
output_schema: {type: object}
risk: read_only
timeout_seconds: 10
idempotency: read
