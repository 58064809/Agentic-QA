schema_version: agentic-qa.harness.skill-manifest.v3
name: ui-testing
description: 通过 allowlisted Playwright MCP 验证 UI 流程。
instructions:
  - id: skill.ui-testing.permission
    kind: safety
    text: UI 状态变更由测试环境、ExecutionProfile 和工具白名单共同授权。
    enforced_by: [validator:execution-profile, allowlist:agent-tool-manifest]
