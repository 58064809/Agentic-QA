# 执行证据

执行状态必须严格区分：

- `passed`：请求或操作完成，且全部必要断言有可追踪证据。
- `failed`：测试目标已执行，观察结果与明确预期不一致。
- `error`：工具、网络、脚本或环境异常导致无法可靠判断产品行为。
- `blocked`：缺少权限、数据、依赖或环境，测试目标未实际完成。

保存环境标识、时间、用例或步骤、输入摘要、实际结果、断言结果及脱敏附件引用。禁止把
`error`、`blocked` 或无断言的响应归类为产品失败。
