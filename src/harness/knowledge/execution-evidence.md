# 执行证据

## 状态判定

| 状态 | 必要条件 |
|---|---|
| `passed` | 操作完成，全部必要断言有可追踪证据 |
| `failed` | 目标已执行，观察结果与明确预期不一致 |
| `error` | 工具、网络、脚本或环境异常，无法判断产品行为 |
| `blocked` | 缺少权限、数据、依赖或环境，目标未完成 |

## 必须保存

- 环境标识、时间、用例或步骤、输入摘要。
- 实际结果、断言结果和脱敏附件引用。

禁止把 `error`、`blocked` 或无断言的响应归类为产品失败。
