# 失败分诊

## 分类顺序

1. 验证证据完整性。
2. 区分产品缺陷、测试、环境、依赖、数据或未知原因。
3. 记录观察事实、期望依据、最小复现、影响范围、候选根因、置信度和下一步。

## Bug Gate

| 条件 | 可否准备 `bug_draft` |
|---|---:|
| `failed` + 稳定复现 + 明确预期 + 产品行为证据 | 是 |
| 根因不足 | 否，保持 `unconfirmed` |
| `error` 或 `blocked` | 否，只输出分诊或阻塞说明 |
