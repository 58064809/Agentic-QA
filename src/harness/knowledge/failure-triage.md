# 失败分诊

先验证证据完整性，再区分产品缺陷、测试问题、环境问题、依赖问题、数据问题和未知原因。
失败分析应包含观察事实、期望依据、最小复现、影响范围、候选根因、置信度和下一步验证。

只有 `failed` 且存在稳定复现、明确预期和产品行为证据时，才可准备 `bug_draft`。根因不足
保持 `unconfirmed`；`error` 和 `blocked` 只能形成分诊或阻塞说明，不能生成 Bug 事实。
