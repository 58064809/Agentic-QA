# QA 报告方法

## 结构

`范围 → 执行 → 结果 → 风险 → 限制 → 建议`

## 规则

- 区分计划覆盖、实际执行和未覆盖。
- 统计回溯到 execution evidence；缺陷与风险引用对应证据，不重复计算。
- 发布建议仅是候选意见，列出 blocker、剩余风险和待人工确认内容。
- 报告停在 `needs_human_review`，不能代表人工批准或触发 promote。
