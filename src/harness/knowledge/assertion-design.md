# 断言与证据设计

断言必须绑定业务预期，而不只是“请求成功”或“元素存在”：

- API：协议状态、业务码、关键字段类型和值域、错误语义、持久化状态和副作用。
- UI：关键文案、控件状态、导航结果、业务状态、稳定等待和失败截图。
- 异步：使用可观测的完成条件、合理超时和轮询边界，不依赖固定睡眠。
- 动态数据：token、业务 ID 等从响应或 fixture 提取；真实凭证只从环境变量读取。

证据应说明来源、观察时间、执行环境和关联步骤。无法取得证据时标记未执行或待确认，不把
预期结果写成实际结果。
