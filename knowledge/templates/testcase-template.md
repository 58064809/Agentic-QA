# 测试用例模板

| 标题 | 优先级 | 前置条件 | 测试步骤 | 预期结果 |
|---|---|---|---|---|
| 手机号密码登录成功返回 Bearer token | P0 | 正常用户账号存在、未锁定、密码正确、登录开关开启 | 1. 调用登录接口<br>2. 提交合法手机号和正确密码<br>3. 检查响应字段 | 返回成功；包含 `access_token`、`token_type=Bearer`、`expires_in`；不返回密码字段 |

## 覆盖矩阵

| 需求/规则 | 覆盖用例 | 覆盖类型 | 状态 |
|---|---|---|---|

## 数量与结构要求

- 表格列固定为：标题、优先级、前置条件、测试步骤、预期结果。
- 不新增“用例类型”列。
- 简单需求不少于 15 条，中等需求不少于 30 条，复杂需求不少于 50 条。
- 每条用例必须具备可执行步骤和可观察预期。

## 未覆盖范围

- 待补充。

## 待人工审核

- [ ] 用例是否覆盖主流程、异常、边界、状态和风险。
- [ ] 预期结果是否可观察。
- [ ] 是否允许基于本用例生成自动化草稿。
