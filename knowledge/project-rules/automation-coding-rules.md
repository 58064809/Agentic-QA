# 自动化编码规则

- 测试代码应结构清晰，夹具、数据和断言分离。
- 不提交真实密钥、手机号、密码或生产 token。
- 失败信息应足以帮助定位接口、参数和预期。
- 对外部依赖使用可配置地址和显式跳过条件。

## pytest 草稿要求

- 使用环境变量读取服务地址和测试账号。
- 缺少环境变量时使用 `pytest.skip`。
- 不在代码中写真实手机号、密码、token、cookie。
- 断言失败消息应包含接口路径和关键响应内容。
