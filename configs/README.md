# Configs

Tracked files in this directory must be examples only.

Use `config.example.yaml` and `model.example.yaml` as templates for local configuration.
Local files such as `configs/local.yaml` and `configs/private.yaml` are ignored by git.
