# API 契约分析技能

## 能力说明

解析接口方法、路径、请求、响应、错误码、鉴权和幂等要求。

## 使用方法

- 对每个接口建立字段和约束表。
- 区分 HTTP 状态码和业务码。
- 标记缺失、冲突或不一致的接口信息。
- 将契约映射为 API 断言。

## 输出检查

- 请求和响应是否完整。
- 错误场景是否覆盖。

## 限制

接口文档与实现不一致时不能自行认定，以证据和人工确认为准。
