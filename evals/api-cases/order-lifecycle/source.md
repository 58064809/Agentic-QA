# Order lifecycle API evaluation

Create an order for both a single-item and a multiple-item dataset. Capture the returned order ID, read that order in a later request, and delete every created order during cleanup. The API token is supplied through `QA_API_TOKEN`; no credential value is part of this source.
