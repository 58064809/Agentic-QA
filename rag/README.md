# RAG

This directory is reserved for retrieval-augmented generation components.

Phase 0 defines only the structure. Later phases may add Markdown loaders, splitters,
embeddings, retrievers, and vector store adapters. Local vector indexes must stay outside git.
