# Apps

This directory contains thin entry-point adapters for running Agentic-QA from local tools.

- `cli/`: command-line entry point wrappers.
- `pycharm/`: PyCharm-friendly run wrappers.
- `feishu_bot/`: reserved for a future Feishu bot integration.

Runtime orchestration remains in `runtime/`; these files should stay thin.
