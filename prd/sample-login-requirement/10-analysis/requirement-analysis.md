---
status: needs_human_review
human_review_required: true
artifact_type: requirement_analysis_draft
generated_by: Codex
---

# 手机号密码登录需求分析草稿

## 需求摘要

本需求描述用户通过手机号和密码登录系统。系统需要校验手机号格式、密码正确性、连续错误次数、账号锁定状态和 token 过期状态。登录成功后返回 token；token 过期后访问受保护资源需要重新登录。

## 业务规则

| 编号 | 规则 | 来源 | 状态 |
|---|---|---|---|
| R1 | 用户使用手机号和密码登录 | `requirement.md` | 待人工审核 |
| R2 | 手机号格式必须符合中国大陆手机号格式 | `requirement.md`、`api-doc.md` | 待人工审核 |
| R3 | 密码错误时提示“手机号或密码错误” | `requirement.md`、`api-doc.md` | 待人工审核 |
| R4 | 同一账号连续输错 5 次后锁定 15 分钟 | `requirement.md` | 待人工审核 |
| R5 | 锁定期间再次登录提示账号锁定并返回剩余锁定时间 | `requirement.md` | 待人工确认 |
| R6 | 登录成功返回 access token、token 类型和过期时间 | `requirement.md`、`api-doc.md` | 待人工审核 |
| R7 | token 过期后访问受保护资源提示重新登录 | `requirement.md`、`api-doc.md` | 待人工审核 |

## 用户流程

1. 用户进入登录入口。
2. 用户输入手机号和密码。
3. 系统校验手机号格式。
4. 系统校验账号状态和密码。
5. 登录成功时返回 token。
6. 后续访问受保护资源时校验 token。
7. token 过期后要求用户重新登录。

## 数据规则

| 数据 | 规则 | 待确认点 |
|---|---|---|
| phone | 中国大陆手机号格式 | 是否限定具体号段 |
| password | 由服务端验证，接口文档未定义复杂度 | 是否需要密码复杂度错误提示 |
| error_count | 连续错误次数达到 5 次触发锁定 | 计数维度是账号、手机号、设备还是 IP |
| lock_until | 锁定 15 分钟 | 是否返回剩余秒数 |
| access_token | 登录成功返回 | token 有效期是否固定 7200 秒 |

## 权限/状态规则

| 状态 | 允许动作 | 禁止动作 | 预期响应 |
|---|---|---|---|
| 未登录 | 登录 | 访问受保护资源 | 未登录或 token 缺失提示 |
| 正常账号 | 使用正确密码登录 | 无 | 返回 token |
| 错误计数中 | 继续尝试登录 | 无 | 错误次数累计 |
| 锁定中 | 等待锁定结束 | 使用密码登录成功 | `ACCOUNT_LOCKED` |
| token 过期 | 重新登录 | 继续访问受保护资源 | `TOKEN_EXPIRED` |

## 异常场景

- 手机号格式错误。
- 密码错误但未达到锁定阈值。
- 第 5 次连续密码错误触发锁定。
- 锁定期间再次登录。
- token 过期后访问受保护资源。
- 接口文档与实现的 HTTP 状态码或业务码不一致。

## 测试风险

| 风险 | 等级 | 说明 | 覆盖建议 |
|---|---|---|---|
| 锁定计数维度不清 | 高 | 影响安全策略和测试数据准备 | 人工确认后设计状态迁移用例 |
| 错误提示安全性 | 中 | 过于明确可能泄露账号状态 | 产品和安全确认 |
| token 有效期不清 | 中 | 影响过期测试和自动化等待策略 | 接口负责人确认 |
| 真实接口环境缺失 | 中 | 当前无法执行真实 API 调用 | pytest 草稿默认 skip |

## 待确认问题

| 编号 | 问题 | 建议确认人 |
|---|---|---|
| Q1 | 锁定计数维度是账号、手机号、设备、IP 还是组合维度？ | 产品负责人/安全负责人 |
| Q2 | 锁定剩余时间是否必须返回，字段名是什么？ | 接口负责人 |
| Q3 | token 有效期是否固定为 7200 秒？是否支持 refresh token？ | 接口负责人 |
| Q4 | 错误响应是否统一使用 HTTP 状态码，还是 HTTP 200 + 业务码？ | 接口负责人 |

## 需求到测试关注点映射

| 需求/规则 | 测试关注点 | 建议优先级 |
|---|---|---|
| 手机号密码登录成功 | 正确账号成功返回 token | P0 |
| 手机号格式校验 | 非法手机号、空手机号、非数字手机号 | P1 |
| 密码错误提示 | 错误密码返回安全错误文案 | P0 |
| 连续错误 5 次锁定 | 第 4 次、第 5 次、第 6 次、锁定恢复 | P0 |
| token 过期 | 受保护接口返回重新登录提示 | P0 |

## 人工审核点

- [ ] 需求理解是否准确。
- [ ] 锁定规则和 token 规则是否完整。
- [ ] 是否允许基于本分析生成测试用例草稿。
