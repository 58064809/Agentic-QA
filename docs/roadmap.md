# 路线图

## 当前已实现

- 测试主管与十个声明式专家 Agent。
- `Harness.run/stream/resume/inspect` 和薄 CLI。
- 动态并行派发、预算、事件、checkpoint 和 partial 降级。
- `workspaces/<id>/` Artifact Store、硬 Review Gate 和原子 promote。
- Agent/Tool manifest、全局 ModelGateway 边界和 MCP allowlist 快照。
- API cases v1.1、execution evidence v1、failure triage v1。

## 后续

- 完整的 Playwright MCP live smoke 环境配置和录制场景扩充。
- LangGraph 持久化后端的 PostgreSQL 运行配置。
- 更多只读测试管理系统连接器；外部缺陷系统写入不在首版范围。
